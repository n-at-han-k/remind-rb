# frozen_string_literal: true

source "https://rubygems.org"

# Listed outright rather than through `gemspec`: bundlerEnv resolves against a
# store directory holding only a Gemfile and a lockfile, where there is no
# gemspec to read. One bundle covers the whole repository -- the bindings, the
# converter beside them, and the tooling for both.

# remind-rb
gem "fiddle", "~> 1.1"

# rem2ics
gem "icalendar", "~> 2.12"
gem "ice_cube", "~> 0.17"

# Tooling
gem "rake", "~> 13.0"
gem "rubocop", "~> 1.88"
gem "scampi", "~> 1.0"
